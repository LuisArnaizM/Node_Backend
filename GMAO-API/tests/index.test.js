// Test suite principal para pruebas unitarias
// Este archivo es solo informativo - los tests se ejecutan individualmente

describe('GMAO API - Test Suite', () => {
  test('Test suite is configured correctly', () => {
    expect(true).toBe(true);
  });
});
